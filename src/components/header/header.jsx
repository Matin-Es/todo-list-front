import Calendar from "../calendar/calendar";
import { StyledDiv } from "./header.style";

const Header = () => {
  return (
    <StyledDiv>
      <Calendar />
    </StyledDiv>
  );
};

export default Header;
